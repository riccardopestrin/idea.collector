// Chiude il modal sulla navigazione client verso "/" (es. redirect dopo il salvataggio):
// default.tsx copre solo il refresh, non la soft-nav. Vedi parallel-routes.md § Modals.
export default function Page() {
  return null;
}
